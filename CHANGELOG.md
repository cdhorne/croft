# Changelog

User-facing changes to the `zonot` CLI (npm package and single-binary). Internal decisions are
tracked in [`docs/adr/CHANGELOG.md`](docs/adr/CHANGELOG.md).

## Unreleased

- Phase 0 scaffold (monorepo + toolchain). Nothing user-facing yet.
