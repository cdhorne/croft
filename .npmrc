# Pnpm config (npm will warn on these unknown keys — safe to ignore).
# node-linker + shamefully-hoist are required by Expo / Metro, which expect a flat
# node_modules layout. Removing them breaks the mobile workspace.
node-linker=node-modules
shamefully-hoist=true
strict-peer-dependencies=false
auto-install-peers=true
