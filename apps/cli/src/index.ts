#!/usr/bin/env bun
// @zonot/cli — the Bun-compiled CLI.
// Phase 2 surface (per ROADMAP / docs/specs/cli-spec.md §1):
//   init / capture / append / correct / undo / delete /
//   read / list / search / tags / workspaces /
//   import / mcp / serve / sync / status / doctor / logs / completion

// Phase 2 scaffolding: the command surface lands as the spec drives.
console.error('zonot: scaffold (Phase 2 not yet built)');
process.exit(3);
