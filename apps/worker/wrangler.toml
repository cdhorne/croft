name = "zonot-worker"
main = "src/index.ts"
compatibility_date = "2026-06-01"
compatibility_flags = ["nodejs_compat"]

# Cloudflare configuration lands as the Worker takes shape (Phase 1).
# See: docs/specs/worker-spec.md
#   - KV bindings for tag vocab + entitlement (rev as v1.1 lands)
#   - Rate limiter binding (ADR-0035 §3.2)
#   - Workers AI binding (v1.2)
#   - Logpush + Analytics Engine bindings
